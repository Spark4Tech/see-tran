# This agent builds or updates a vendor using a search LLM
# Gets data, description, news, contacts, web assets, logo, builds header image
# Maps sourced components and functions supported (calls component_agent)